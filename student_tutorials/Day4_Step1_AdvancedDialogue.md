# Day 4: Advanced Dialogue and Branching

Welcome to Day 4! Let's build a branching conversation option.

## Step 1: Create a Branching Conversation
**File to edit:** `dialogue_system.py`
**Search for:** `@STUDENT-EDIT-Day4-1`

Right now, the fallback just returns a single string. Let's make it more interesting!
**Hint:** Instead of returning a string from `fallbacks.get()`, you can return a list of strings if you want multiple dialogue pages, or implement a prompt for user input (in the console) to determine which branch to take! For now, try modifying the fallback dictionary to hold a list of sentences so the dialogue box spans multiple pages.
